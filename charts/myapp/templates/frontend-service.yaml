apiVersion: v1
kind: Service
metadata:
  name: myapp-frontend
spec:
  type: NodePort
  selector:
    app: myapp-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: {{ .Values.environment.frontendPort }}
      nodePort: {{ .Values.frontend.service.nodePort }}
